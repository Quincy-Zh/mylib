CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

SET(LIB_PLC plc)

INCLUDE_DIRECTORIES(mitsubishi)

SET(LIB_PLC_SOURCES 
  plc.c
  mitsubishi/fx.c
  )

ADD_LIBRARY(${LIB_PLC} SHARED ${LIB_PLC_SOURCES})
